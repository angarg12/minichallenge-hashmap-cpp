#ifndef MINICHALLENGE_HASHMAP
#define MINICHALLENGE_HASHMAP

#include <vector>

class Solution {
public:
    std::vector<int> twoSum(std::vector<int>& nums, int target);
    std::vector<int> twoSumFast(std::vector<int>& nums, int target);
};

#endif

